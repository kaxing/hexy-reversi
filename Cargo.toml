[package]
name = "hexy-reversi"
version = "0.1.0"
edition = "2024"

[dependencies]
gpui = { version = "0.2.2", default-features = false, features = ["macos-blade", "font-kit"] }

[[bin]]
name = "hexy-reversi"
path = "main.rs"
